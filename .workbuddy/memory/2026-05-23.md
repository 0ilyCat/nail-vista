# 2026-05-23 — NailVista

## UI 细节修复
- **blob 球体截断修复**: 移除 `chat-page-sidebar` 的 `overflow: hidden`（改到背景层），按钮加 `overflow: visible`，两个 blob（44px 悬浮款 + 140px 对话页款）加 `filter: blur()` 柔化边缘
- **试戴加载文字修复**: "油画质感渲染中，请稍候" → "正在生成AI试戴效果图"
