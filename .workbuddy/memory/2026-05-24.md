# 2026-05-24 — NailVista

## Blob 截断根因修复
- **根因**: `filter: blur()` 在矩形边界处渐变消失反而强化"框"感
- **修复**: 移除所有 blob 的 `filter: blur`，改用 `transform: scale()` 脉冲动画 + `box-shadow` 柔光
- `blobMorph` keyframes 增加 scale: 0%→1.0, 20%→1.08, 40%→1.03, 60%→1.06, 80%→1.02, 100%→1.0
- `chat-page-sidebar` 加 `overflow: visible`

## TryOnPage 紧凑一页布局

## TryOnPage 回退
- TryOnPage 回退到三栏等宽原始版本（gradient-border-subtle 卡片 + 油画生成动画 + floating ask button）
- 紧凑布局 CSS（tryon-compact 系列）保留在 index.css 中但暂未使用

## 预览图裁切 + 滚动区收窄
- `.preview-area-gradient img` `object-fit: contain` → `cover`，手图/款式大图填满正方形（与下方缩略图一致）
- 历史记录和款式滚动 `maxHeight: 160` → `120`（正好两行 56px 缩略图）
- 效果图保持原比例不变

## 预览图 selector 修复 + 预览缩小
- `.preview-area-gradient` 选择器被插入 CSS 时误删，导致 `aspect-ratio: 1` 成孤儿规则，已修复
- 手图/款式预览加 `maxHeight: 200px`，`marginInline: auto` 居中
- `object-fit` 恢复 `contain`，历史/款式 `maxHeight` 恢复 160px

## ChatPage + AI试戴按钮
- ChatPage 左侧 sidebar 底部新增 `.btn-gradient` 风格 AI试戴按钮，点击跳转 `/user/tryon`
- 新增 `.btn-gradient` 全局工具类：彩色渐变背景 + gradientBorder 动画 + glow hover

## 运营页面 + 对话页面修复
- **DashboardLayout padding** 32px → 12px/16px，`overflow: hidden`
- **Dashboard CSS**: `.dashboard-left-panel` 去掉 `overflow-y: auto`，`.dashboard-main-layout` 加 `overflow: hidden`，仅排行榜 `.dashboard-ranking-table` 保留滚动
- **DashboardPage**: 删除排行榜下方 quick actions 按钮（与对话框重复）
- **TryOnPage + DashboardPage**: 删除所有 `gradient-border-subtle` 类（绿色渐变线条不齐）
- **欢迎语 Markdown**: DashboardPage 和 ChatPage 的 `•` 改为 `- `，修复列表渲染
- **UserLayout padding** 32px → 12px/16px

## 验证
- TypeScript 零错误

## 运营+AI试戴细节修复
- **Dashboard**: 删除"运营看板"标题，整体上移；`dashboard-main-layout` height: `calc(100vh - 80px)`
- **排行榜**: `max-height: calc(100vh - 500px)` 自适应，`overflow-x: hidden`；NaN% 改为 `--` 显示
- **欢迎语 Markdown**: 列表前加 `\n\n`，确保 ReactMarkdown 正确渲染 `<ul>`
- **TryOnPage**: 整体高度 `calc(100vh - 56px - 24px)`，卡片 `bodyStyle: flex column` 撑满
- **大图**: 去掉 `maxHeight: 200` 限制，`flex: 1` 自动撑大
- **历史/款式**: `maxHeight: 160` → `120`（正好两行）
- **所有卡片**: 内边距缩减 `padding: 8px 16px 12px`

## TryOnPage 重构（玻璃质感·连接箭头·三图等大）
- **彻底重写**: 去掉 antd Card，改用自定义 div + CSS
- **三图流动线**: 手图 → `+` 圆形箭头 → 款式 → `=` 圆形箭头 → 效果
- **面板**: `.tryon-panel` 圆角卡片 `shadow-md`，顶部标签行，图片区 `object-fit: cover` 填满
- **无黑边**: 空态背景 `var(--bg-alt)`，有图背景 `oklch(18%)` 深色无纯黑
- **底部控件**: 上传+历史 | 款式搜索+缩略图 | CTA按钮，三栏 flex 布局
- **缩略图**: 44×44 紧凑，hover 放大，选中带 glow
- **加载动画**: 油画 blob 内嵌在面板图片区内
- **新增 CSS**: `.tryon-revamp`, `.tryon-flow`, `.tryon-panel`, `.tryon-connector`, `.tryon-controls` 等 ~15 个类

## Markdown 渲染 + 按钮 hover 修复 (18:04)
- **欢迎语 Markdown**: ChatPage 和 DashboardPage 的 welcomeMessage 从 `\n` 转义字符串改为模板字符串（`` ` ``），确保 ReactMarkdown 正确识别换行和列表
- **remark-breaks**: 安装 `remark-breaks` 插件，单 `\n` 也渲染为 `<br>`
- **AI试戴按钮**: `.btn-gradient` 添加 `:hover/:focus/:active` 状态的 `background` 和 `color` 覆盖（`!important`），防止 antd Button 默认样式冲掉渐变背景
- 截图验证: chatpage-fix.png, dashboard-fix.png, styles-fix.png

## 动画重做 + 小美截断 + 间距对齐修复 (18:15)
- **加载动画重做**: `paint-blob`（4个彩色圆）→ `paint-ribbon`（5条交织光带），使用 `mix-blend-mode: screen` + 独立浮动动画（`ribbonFloat1/2/3`），色彩更炫酷的融合交织效果
- **小美 blob 截断**: `chat-page-blob` 140px→110px, `blobMorph` scale 1.08→1.04，不再被边界裁切
- **AI试戴虚线框**: `.tryon-panel-empty` 加 `aspect-ratio: 1` + `border: 2px dashed ...`，空态和实际图片大小一致
- **ChatPage 底部间距**: `.chat-page-layout` height `calc(100vh - 56px - 96px)` → `calc(100vh - 56px - 16px)`
- **Dashboard 排行榜对齐**: 新 `.dashboard-ranking-card` flex:1 撑满，与右侧对话面板底边对齐；旧 `max-height: calc(100vh - 500px)` 移除
- 截图: tryon-empty-fix.png, chat-xiaomei-fix.png, dashboard-align-fix.png

## 滚动修复 + 面板放大 + 模板提问 + PPT/报告 (18:31)
- **排行榜滚动修复**: `.dashboard-ranking-card` 链式添加 `min-height: 0`（flex overflow 经典问题）
- **试戴面板放大**: TryOnPage 去掉 `aspectRatio: 'auto'` 内联样式，`.preview-area-gradient` 加 `min-width: 220px`
- **ChatPage 模板提问**: 添加 6 个 `templateQuestions` 按钮（推荐春季/爆款/红色系/法式/搭配/渐变）
- **PPT 生成**: `output/NailVista-项目展示.pptx` - 10 页可编辑 PPT，warm rose 配色，OpenClaw 为重点（双 Agent 架构 + 配置 + 流式链路），5 张前端截图嵌入
- **技术报告**: `output/NailVista-技术报告书.md` - 9 章节技术文档
