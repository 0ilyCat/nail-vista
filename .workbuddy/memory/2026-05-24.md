# 2026-05-24 — NailVista

## Blob 截断根因修复
- **根因**: `filter: blur()` 在矩形边界处渐变消失反而强化"框"感
- **修复**: 移除所有 blob 的 `filter: blur`，改用 `transform: scale()` 脉冲动画 + `box-shadow` 柔光
- `blobMorph` keyframes 增加 scale: 0%→1.0, 20%→1.08, 40%→1.03, 60%→1.06, 80%→1.02, 100%→1.0
- `chat-page-sidebar` 加 `overflow: visible`

## TryOnPage 紧凑一页布局

## TryOnPage 回退
- TryOnPage 回退到三栏等宽原始版本（gradient-border-subtle 卡片 + 油画生成动画 + floating ask button）
- 紧凑布局 CSS（tryon-compact 系列）保留在 index.css 中但暂未使用

## 预览图裁切 + 滚动区收窄
- `.preview-area-gradient img` `object-fit: contain` → `cover`，手图/款式大图填满正方形（与下方缩略图一致）
- 历史记录和款式滚动 `maxHeight: 160` → `120`（正好两行 56px 缩略图）
- 效果图保持原比例不变
- **新布局**: `grid: 220px sidebar + 1fr main(3 cols)`
- **Sidebar**: 上传区(紧凑) + 历史记录(单列纵向滚动) + 款式搜索 + 款式列表(单列纵向)
- **主面板**: 3 图并排(手图/款式/效果) + 底部跨列 CTA
- **收紧**: 面板标题 12px，内边距减半，`max-height: calc(100vh - header)` 一页装下
- **CTA 按钮**: `.tryon-cta-gradient` 彩色渐变背景 + glow box-shadow + hover 上浮
- 新增 CSS: `.tryon-compact`, `.tryon-sidebar`, `.tryon-slim-upload`, `.tryon-section-label`, `.tryon-sidebar-list`, `.tryon-thumb`, `.tryon-main-panels`, `.tryon-panel`, `.tryon-panel-title`, `.tryon-image-box`, `.tryon-image-placeholder`, `.tryon-cta-gradient`, `.btn-gradient`

## ChatPage + AI试戴按钮
- ChatPage 左侧 sidebar 底部新增 `.btn-gradient` 风格 AI试戴按钮，点击跳转 `/user/tryon`
- 新增 `.btn-gradient` 全局工具类：彩色渐变背景 + gradientBorder 动画 + glow hover

## 验证
- TypeScript 零错误
- 3 页面 Playwright 截图确认
