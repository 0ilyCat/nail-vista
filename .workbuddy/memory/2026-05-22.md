# 2026-05-22~23 — NailVista UI Overhaul + OpenClaw Integration

## Branch
- Created `feat/ui-overhaul` for all changes

## 2026-05-23 OpenClaw 404 修复
- **根因**: 全局 `~/.openclaw/openclaw.json` 缺 `gateway.http.endpoints.chatCompletions.enabled: true`
- **修复**: 在全局 config 添加 `http.endpoints.chatCompletions.enabled: true` + `auth.token: nailvista-dev-token`
- **验证**: `/v1/models` → JSON agent 列表; `/v1/chat/completions` → 小美/运营助手均正常回复
- **API Key**: 直接写入全局 config 的 `env.MIMO_API_KEY` 和 `apiKey` 字段，不再依赖环境变量
- **项目 config** (`openclaw/openclaw.json`): 模板供队友使用，Key 为占位符

## Backend Changes
- Added `backend/app/models/chat.py` — ChatSession, ChatMessage, ScheduledTask models
- Added `backend/app/api/chat.py` — SSE streaming endpoints:
  - `/api/chat/user` & `/api/chat/user/stream` — user-side chat (小美)
  - `/api/chat/dashboard` & `/api/chat/dashboard/stream` — dashboard chat (运营助手)
  - `/api/chat/sessions` — session listing & history
  - `/api/chat/tasks` — scheduled task CRUD
- Updated `main.py` to register chat routes and import chat models
- Installed `httpx` for async OpenClaw proxy calls
- Backend running on port 8190, verified all endpoints

## OpenClaw Setup
- Created 2 agent workspaces:
  - `~/.openclaw/workspace-nailvista-xiaomei/` — "小美" user assistant
  - `~/.openclaw/workspace-nailvista-ops/` — operations analyst
- 4 skills for 小美: nail-style-search, nail-style-recommend, nail-tryon-evaluate, nail-trend-chat
- 5 skills for 运营助手: ops-overview, ops-trends, ops-hot-styles, ops-generate-report, ops-schedule-task
- Each agent has SOUL.md defining personality and boundaries

## Frontend Changes (all 15+ files)
- **CSS overhaul**: Replaced flat pink with warm rose oklch palette, added Playfair Display + DM Sans fonts, 4pt spacing scale
- **Routing**: Removed sidebar MainLayout, added RoleSelectPage (/) → UserLayout (/user/*) + DashboardLayout (/dashboard/*)
- **ChatWidget**: Reusable SSE streaming chat component with markdown rendering, tool call collapsible boxes, thinking display, quick actions
- **RoleSelectPage**: Two large cards (User/Dashboard) with brand area
- **UserLayout**: Minimal header with nav (styles/tryon/dashboard/home)
- **DashboardLayout**: Minimal header with live tryon count badge
- **StyleBrowsePage**: Added AI试戴 overlay button on each card, navigate to /user/tryon?styleId=X
- **TryOnPage**: 3-column equal-width CSS Grid layout with large preview areas, embedded 小美 ChatWidget at bottom, pre-select style from URL param
- **DashboardPage**: Replaced old chat panel with ChatWidget + 4 quick action buttons
- **NotFoundPage**: 404 page with navigation back
- Deleted old MainLayout.tsx
- Installed react-markdown, remark-gfm, rehype-raw
- TypeScript: clean, 0 errors
- Build: vite build succeeds

## Screenshots
4 screenshots taken in screenshots/:
- homepage-role-select.png
- user-styles.png
- user-tryon.png
- dashboard.png

## MiMo Model Configuration (2026-05-22 16:50)
- Updated `~/.openclaw/openclaw.json`:
  - Renamed provider from `xiaomi` to `xiaomi-coding` (avoids conflict with built-in MiMo gateway)
  - Models: mimo-v2.5-pro (text, 1M ctx, reasoning), mimo-v2.5 (text+image, 262K ctx, reasoning), mimo-v2-omni (text+image, reasoning)
  - Default agent model: `xiaomi-coding/mimo-v2.5-pro`
  - Registered agents in config: nailvista-xiaomei, nailvista-ops
  - Config validated: `openclaw config validate` OK
- Updated nail-tryon-evaluate SKILL.md to reference MiMo V2.5 multimodal model
- API Key placeholder remains in env.MIMO_API_KEY (user will fill)

## Project-level OpenClaw + Conda Env Fix (2026-05-22 22:55)
- **Conda env**: `meituan-hackathon` (D:\anaconda3\envs\meituan-hackathon, Python 3.12.13)
  - Installed: fastapi, uvicorn, sqlalchemy, aiosqlite, httpx, pydantic-settings, opencv-python, mediapipe==0.10.18, openai
- **Project OpenClaw config**: Moved to `openclaw/openclaw.json` with:
  - Provider `xiaomi-coding` (MiMo Token Plan)
  - gateway.http.endpoints.chatCompletions.enabled: true (was root cause of 404)
  - gateway.auth: token mode with `nailvista-dev-token`
  - Agent workspaces point to `openclaw/agents/xiaomei/` and `openclaw/agents/ops/`
- **API Key**: Referenced via env var `MIMO_API_KEY` (SecretRef), not in config
- **Backend .env**: Added `OPENCLAW_GATEWAY_TOKEN=nailvista-dev-token`
- **Backend config.py**: Added `OPENCLAW_BASE_URL` and `OPENCLAW_GATEWAY_TOKEN` fields
- **Backend chat.py**: Uses settings for OpenClaw URL/auth instead of hardcoded
- **Startup**: `start-openclaw.bat` for easy gateway launch
- **Git-safe**: `.env.example` for reference, actual keys in `.env` (gitignored), `openclaw/agents/*/state/` in gitignore
- **Validation**: `openclaw config validate` passes; `/v1/models` returns agent list; `/v1/chat/completions` returns structured error (not 404 — needs valid key)
- **MEMORY.md**: Written with full project conventions

## UI 质感提升 + 布局优化 (2026-05-22 23:17)
- **悬浮"问问小美"按钮**: 新建 `FloatingAskButton.tsx`，fixed 左下角，左侧彩色变换 blob 球体 + "问问小美"渐变文字，hover 上浮+阴影
- **独立 AI 对话页**: 新建 `ChatPage.tsx`，路由 `/user/chat`，左侧小美形象区（blob 球体+名字+简介），右侧全屏 ChatWidget
- **TryOnPage 瘦身**: 去掉底部 ChatWidget 卡片，试戴加载态替换为 4 个彩色 blob 油画质感变换动画（paint-blob + gen-strokes + 渐变遮罩），结果用 fadeSlideIn 过渡
- **StyleBrowsePage**: 加 FloatingAskButton
- **UserLayout**: 导航栏加"对话小美"按钮（MessageOutlined），当前页高亮
- **Dashboard 布局优化**: 
  - 趋势图 280→200px 高度
  - 统计卡片替换为 stat-card-gradient（顶部彩色渐变线条 + 光晕）
  - 排行榜固定 max-height 480px 滚动
  - 排行榜+聊天助手改为 flex 横向布局（排行 flex:1，聊天 360px 固定宽）
  - 卡片加 gradient-border-subtle 渐变边框
- **CSS 新增动效**: blobMorph/bloblColorShift/blobGlowPulse 变换球体、paintStroke/paintColorCycle/paintShimmer 油画生成、gradientBorder 渐变边框动画、preview-area-gradient 预览渐变边框
- **验证**: TypeScript 零错误通过，4 页面 Playwright 截图确认
